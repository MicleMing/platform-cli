#!/usr/bin/env node

var program = require('commander');

program
    .version(require('../package').version)
    .usage('<command> [options]')
    .command('init', 'generate a new project from a template')
    .command('make', 'create boilerplate for project ')
    .command('test', 'test')
    .command('mock', 'set up a mock server ')
    .parse(process.argv);